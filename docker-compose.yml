version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    command: sh -c "sleep 10 && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
    environment:
      - POSTGRES_DB=vin
      - POSTGRES_USER=vin
      - POSTGRES_PASSWORD=AVNS_NEKTfBE73pqxvihfW7t
      - POSTGRES_HOST=app-c74d3a38-13c8-48a9-ba14-e53c8e815a65-do-user-15225862-0.c.db.ondigitalocean.com
      - POSTGRES_PORT=25060

  db:
    image: postgres:13.0-alpine
    environment:
      POSTGRES_DB: vin_cod
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 5625
    ports:
      - "5434:5432"

  redis:
    image: redis:alpine
    ports:
      - 6379:6379
